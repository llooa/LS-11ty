<nav class="navbar navbar-expand navbar-dark bg-dark mb-3">
  <div class="container">
    <a href="{{'/' | url }}" class="navbar-brand">
     <i class="fas fa-cube mr-2"></i>&nbsp;LS-11ty
    </a>

    <ul class="navbar-nav">
      <li class="nav-item">
        <a href="{{'/' | url }}" class="nav-link"><i class="fas fa-home"></i></a>
      </li>
      <li class="nav-item">
        <a href="{{'/posts/0' | url }}" class="nav-link">Posts</a>
      </li>     
      {% set mainNav = collections.all | eleventyNavigation('main') %}
      {%- for nav in mainNav -%}
        <li class="nav-item">
          <a href="{{nav.url | url}}" class="nav-link">{{nav.key}}</a>
        </li>             
      {%- endfor -%}      
      <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            More
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
          {%- for socialNav in social -%}
            <li><a class="dropdown-item" target="_blank" href="{{socialNav.url}}">
              <i class="{{socialNav.iconClass}} text-secondary mr-2"></i>&nbsp;{{socialNav.name | capitalize}}</a>
            </li>
          {%- endfor -%}            
          </ul>
      </li>               
    </ul>
  </div>
</nav>